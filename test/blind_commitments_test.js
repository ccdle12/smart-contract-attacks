contract('blind_commitments_test', function(accounts) {
  it("should assert true", function(done) {
    var blind_commitments_test = blind_commitments_test.deployed();
    assert.isTrue(true);
    done();
  });
});
